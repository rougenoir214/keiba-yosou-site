<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>レース結果インポート</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 800px; margin: 0 auto; }
        .card {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
        h1 { color: #333; margin-bottom: 20px; }
        h2 { color: #667eea; margin-bottom: 15px; font-size: 20px; }
        .form-group { margin-bottom: 20px; }
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #555;
        }
        input[type="file"] {
            width: 100%;
            padding: 10px;
            border: 2px solid #e0e0e0;
            border-radius: 5px;
        }
        .btn {
            padding: 12px 24px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .btn:hover { background: #5568d3; }
        .back-link {
            display: inline-block;
            margin-top: 15px;
            color: #667eea;
            text-decoration: none;
        }
        .back-link:hover { text-decoration: underline; }
        .info-box {
            background: #f0f4ff;
            padding: 15px;
            border-radius: 5px;
            margin-top: 15px;
            color: #555;
        }
        .info-box h3 { color: #667eea; margin-bottom: 10px; }
        .info-box code {
            background: white;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: monospace;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>🏁 レース結果インポート</h1>
            
            <h2>1. 着順データをインポート</h2>
            <form action="/admin/import-results" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="resultsfile">race_results.csv</label>
                    <input type="file" id="resultsfile" name="resultsfile" accept=".csv" required>
                </div>
                <button type="submit" class="btn">着順データをインポート</button>
            </form>
            
            <div class="info-box">
                <h3>📋 CSVフォーマット（着順）</h3>
                <p>列: <code>race_id, 着順, 馬番, 馬名, タイム</code></p>
                <p>例: <code>202606010711, 1, 5, ダノンプレミアム, 2:24.1</code></p>
            </div>
        </div>

        <div class="card">
            <h2>2. 払戻金データをインポート</h2>
            <form action="/admin/import-payouts" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="payoutsfile">race_payouts.csv</label>
                    <input type="file" id="payoutsfile" name="payoutsfile" accept=".csv" required>
                </div>
                <button type="submit" class="btn">払戻金をインポート</button>
            </form>
            
            <div class="info-box">
                <h3>💰 CSVフォーマット（払戻金）</h3>
                <p>列: <code>race_id, 馬券種別, 組み合わせ, 払戻金</code></p>
                <p>例: <code>202606010711, tansho, 5, 540</code></p>
                <p>馬券種別: tansho, fukusho, umaren, umatan, wide, sanrenpuku, sanrentan</p>
            </div>
        </div>

        <div class="card">
            <h2>📝 使い方</h2>
            <ol style="line-height: 1.8; color: #555;">
                <li>スクレイピングスクリプトを実行: <code>python scrape_results.py</code></li>
                <li>レースIDを入力（確定済みのレース）</li>
                <li>生成された <code>race_results.csv</code> と <code>race_payouts.csv</code> をインポート</li>
                <li>レース一覧から結果を確認</li>
            </ol>
            
            <a href="/admin/import" class="back-link">← レースデータインポート</a><br>
            <a href="/races" class="back-link">← レース一覧</a>
        </div>
    </div>
</body>
</html>
