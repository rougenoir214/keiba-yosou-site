<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ランキング - 競馬予想共有サイト</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; padding: 20px; }
.container { max-width: 1200px; margin: 0 auto; }
.header { background: white; padding: 20px; border-radius: 10px; margin-bottom: 20px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); }
.header h1 { color: #333; margin-bottom: 10px; }
.back-link { display: inline-block; margin-top: 10px; color: #667eea; text-decoration: none; }
.back-link:hover { text-decoration: underline; }

/* タブ */
.tabs { display: flex; gap: 10px; margin-top: 15px; border-bottom: 2px solid #e0e0e0; }
.tab { padding: 10px 20px; cursor: pointer; border: none; background: none; font-size: 16px; color: #888; transition: all 0.3s; }
.tab.active { color: #667eea; border-bottom: 3px solid #667eea; font-weight: bold; }
.tab:hover { color: #667eea; }

.season-selector { margin-top: 15px; }
.season-selector select { padding: 8px; border: 2px solid #e0e0e0; border-radius: 5px; font-size: 14px; }

.section { background: white; padding: 20px; border-radius: 10px; margin-bottom: 20px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); }
.section h2 { color: #333; margin-bottom: 15px; border-bottom: 2px solid #667eea; padding-bottom: 10px; }
table { width: 100%; border-collapse: collapse; }
th { background: #667eea; color: white; padding: 12px; text-align: left; }
td { padding: 12px; border-bottom: 1px solid #eee; }
tr:hover { background: #f5f5f5; }
.rank-1 { background: #ffd700 !important; font-weight: bold; }
.rank-2 { background: #c0c0c0 !important; }
.rank-3 { background: #cd7f32 !important; }
.profit-positive { color: #4caf50; font-weight: bold; }
.profit-negative { color: #f44336; font-weight: bold; }

/* スマホ対応 */
@media (max-width: 768px) {
    body { padding: 10px; }
    .header, .section { padding: 15px; }
    .header h1 { font-size: 1.3rem; }
    .back-link { display: block; margin: 5px 0; }
    .tabs { flex-wrap: wrap; }
    .tab { padding: 8px 15px; font-size: 14px; }
    .season-selector { margin-top: 10px; }
    .season-selector select { width: 100%; font-size: 16px; }
    table { font-size: 0.85rem; display: block; overflow-x: auto; -webkit-overflow-scrolling: touch; }
    th, td { padding: 8px 4px; white-space: nowrap; }
}
</style>
</head>
<body>
<div class="container">
<div class="header">
<h1>🏆 ランキング</h1>

<!-- タブ切り替え -->
<div class="tabs">
    <button class="tab <%= mode === 'monthly' ? 'active' : '' %>" onclick="location.href='/ranking?mode=monthly'">
        📅 月間ランキング
    </button>
    <button class="tab <%= mode === 'season' ? 'active' : '' %>" onclick="location.href='/ranking?mode=season'">
        🏅 シーズンランキング
    </button>
</div>

<!-- シーズン選択（シーズンモードの時のみ表示） -->
<% if (mode === 'season') { %>
<div class="season-selector">
<label for="season">シーズン選択：</label>
<select id="season" onchange="location.href='/ranking?mode=season&season_id='+this.value">
<% seasons.forEach(season => { %>
<option value="<%= season.id %>" <%= selectedSeason && selectedSeason.id === season.id ? 'selected' : '' %>>
<%= season.name %>
</option>
<% }); %>
</select>
</div>
<% } %>

<% if (mode === 'monthly' && currentMonth) { %>
<p style="margin-top: 10px; color: #667eea; font-weight: bold;">
    📊 <%= currentMonth.year %>年<%= currentMonth.month %>月のランキング
</p>
<% } %>

<a href="/" class="back-link">← トップページ</a>
<a href="/races" class="back-link">← レース一覧</a>
</div>

<div class="section">
<h2>🎖️ ユーザーランキング</h2>
<% if (rankings.length === 0) { %>
<p style="color: #888;">まだデータがありません</p>
<% } else { %>
<table>
<thead>
<tr>
<th>順位</th>
<th>ユーザー</th>
<th>レース数</th>
<th>購入額</th>
<th>払戻額</th>
<th>収支</th>
<th>的中率</th>
<th>回収率</th>
</tr>
</thead>
<tbody>
<% rankings.forEach((rank, index) => { %>
<tr class="<%= index < 3 ? `rank-${index + 1}` : '' %>">
<td><strong><%= index + 1 %></strong></td>
<td><%= rank.display_name %></td>
<td><%= rank.race_count %></td>
<td><%= parseInt(rank.total_bet).toLocaleString() %>円</td>
<td><%= parseInt(rank.total_payout).toLocaleString() %>円</td>
<td class="<%= parseInt(rank.profit) >= 0 ? 'profit-positive' : 'profit-negative' %>">
<%= parseInt(rank.profit) >= 0 ? '+' : '' %><%= parseInt(rank.profit).toLocaleString() %>円
</td>
<td><%= rank.hit_rate %>%（<%= rank.hit_race_count %>/<%= rank.race_count %>）</td>
<td><%= rank.recovery_rate %>%</td>
</tr>
<% }); %>
</tbody>
</table>
<% } %>
</div>

</div>
</body>
</html>
