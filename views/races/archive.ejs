<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>過去レース - 競馬予想共有サイト</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 1200px; margin: 0 auto; }
        .header {
            background: white;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        .header h1 { color: #333; margin-bottom: 15px; }
        .filter-section {
            background: #f5f5f5;
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
        }
        .filter-row {
            display: flex;
            gap: 15px;
            align-items: end;
            flex-wrap: wrap;
        }
        .filter-group {
            flex: 1;
            min-width: 200px;
        }
        .filter-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
            color: #555;
        }
        .filter-group select,
        .filter-group input {
            width: 100%;
            padding: 8px;
            border: 2px solid #e0e0e0;
            border-radius: 5px;
            font-size: 14px;
        }
        .btn {
            padding: 10px 20px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            text-decoration: none;
            display: inline-block;
        }
        .btn:hover { background: #5568d3; }
        .btn-secondary {
            background: #888;
        }
        .btn-secondary:hover {
            background: #777;
        }
        .nav-links {
            margin-top: 10px;
        }
        .nav-links a {
            display: inline-block;
            margin-right: 15px;
            color: #667eea;
            text-decoration: none;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .race-card {
            background: white;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 15px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            transition: transform 0.2s;
        }
        .race-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        .race-card h3 {
            color: #333;
            margin-bottom: 10px;
        }
        .race-info {
            display: flex;
            gap: 20px;
            margin-bottom: 15px;
            flex-wrap: wrap;
        }
        .race-info span {
            color: #666;
            font-size: 14px;
        }
        .race-actions {
            display: flex;
            gap: 10px;
        }
        .empty-state {
            background: white;
            padding: 40px;
            border-radius: 10px;
            text-align: center;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📚 過去レース</h1>
            
            <div class="filter-section">
                <form method="GET" action="/races/archive">
                    <div class="filter-row">
                        <div class="filter-group">
                            <label for="season">シーズン選択</label>
                            <select id="season" name="season_id" onchange="this.form.submit()">
                                <option value="">全期間</option>
                                <% seasons.forEach(season => { %>
                                <option value="<%= season.id %>" <%= selectedSeason && selectedSeason.id === season.id ? 'selected' : '' %>>
                                    <%= season.name %>
                                </option>
                                <% }); %>
                            </select>
                        </div>
                        
                        <div style="text-align: center; padding-top: 25px; color: #888;">または</div>
                        
                        <div class="filter-group">
                            <label for="start_date">開始日</label>
                            <input type="date" id="start_date" name="start_date" value="<%= customPeriod ? customPeriod.start_date : '' %>">
                        </div>
                        
                        <div class="filter-group">
                            <label for="end_date">終了日</label>
                            <input type="date" id="end_date" name="end_date" value="<%= customPeriod ? customPeriod.end_date : '' %>">
                        </div>
                        
                        <button type="submit" class="btn">検索</button>
                        <a href="/races/archive" class="btn btn-secondary">リセット</a>
                    </div>
                </form>
            </div>
            
            <div class="nav-links">
                <a href="/">← トップページ</a>
                <a href="/races">← 直近レース一覧</a>
                <a href="/ranking">ランキング</a>
            </div>
        </div>

        <% if (races.length === 0) { %>
            <div class="empty-state">
                <h2>該当するレースがありません</h2>
                <p>別の期間を選択してください</p>
            </div>
        <% } else { %>
            <div style="background: white; padding: 15px 20px; border-radius: 10px; margin-bottom: 15px;">
                <strong><%= races.length %></strong> 件のレースが見つかりました
            </div>
            
            <% races.forEach(race => { %>
                <div class="race-card">
                    <h3><%= race.race_name %></h3>
                    <div class="race-info">
                        <span>📅 <%= new Date(race.race_date).toLocaleDateString('ja-JP') %></span>
                        <span>🕒 <%= race.race_time %></span>
                        <span>📍 <%= race.venue %></span>
                        <span>🏇 <%= race.horse_count %>頭立て</span>
                    </div>
                    <div class="race-actions">
                        <a href="/races/<%= race.race_id %>" class="btn">出馬表を見る</a>
                        <a href="/races/<%= race.race_id %>/result" class="btn" style="background: #4caf50;">結果を見る</a>
                    </div>
                </div>
            <% }); %>
        <% } %>
    </div>
</body>
</html>
