<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ユーザー管理 - 競馬予想サイト</title>
  <%- include('../pwa_head') %>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      background: white;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }

    h1 {
      color: #333;
      margin-bottom: 10px;
      text-align: center;
    }

    .subtitle {
      color: #666;
      text-align: center;
      margin-bottom: 30px;
      font-size: 14px;
    }

    .back-link {
      display: inline-block;
      margin-bottom: 20px;
      color: #667eea;
      text-decoration: none;
      font-size: 14px;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }

    thead {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    th, td {
      padding: 12px;
      text-align: left;
      border-bottom: 1px solid #e0e0e0;
    }

    th {
      font-weight: 600;
      font-size: 14px;
    }

    td {
      font-size: 14px;
    }

    tbody tr:hover {
      background-color: #f8f9fa;
    }

    .badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 600;
    }

    .badge-admin {
      background-color: #ff9800;
      color: white;
    }

    .badge-user {
      background-color: #4caf50;
      color: white;
    }

    .btn {
      padding: 8px 16px;
      border: none;
      border-radius: 5px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-reset {
      background-color: #f44336;
      color: white;
    }

    .btn-reset:hover {
      background-color: #d32f2f;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(244, 67, 54, 0.3);
    }

    .user-count {
      background-color: #e3f2fd;
      border-left: 4px solid #2196f3;
      padding: 12px 15px;
      margin-bottom: 20px;
      border-radius: 6px;
      font-size: 14px;
      color: #1976d2;
    }

    @media (max-width: 768px) {
      .container {
        padding: 20px;
      }

      table {
        font-size: 12px;
      }

      th, td {
        padding: 8px;
      }

      .btn {
        padding: 6px 12px;
        font-size: 12px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>👥 ユーザー管理</h1>
    <p class="subtitle">登録ユーザーの一覧とパスワード初期化</p>
    <%- include('../common_nav', { currentPage: 'admin', user: user }) %>

    <div class="user-count">
      📊 登録ユーザー数: <%= users.length %> 人
    </div>

    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>ユーザー名</th>
          <th>表示名</th>
          <th>権限</th>
          <th>登録日</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(u => { %>
        <tr>
          <td><%= u.id %></td>
          <td><%= u.username %></td>
          <td><%= u.display_name %></td>
          <td>
            <% if (u.is_admin) { %>
              <span class="badge badge-admin">管理者</span>
            <% } else { %>
              <span class="badge badge-user">一般</span>
            <% } %>
          </td>
          <td><%= new Date(u.created_at).toLocaleDateString('ja-JP') %></td>
          <td>
            <button 
              class="btn btn-reset" 
              onclick="resetPassword(<%= u.id %>, '<%= u.display_name %>')"
            >
              🔑 パスワード初期化
            </button>
          </td>
        </tr>
        <% }); %>
      </tbody>
    </table>

    <a href="/" class="back-link">← トップページに戻る</a>
  </div>

  <script>
    async function resetPassword(userId, displayName) {
      if (!confirm(`${displayName} さんのパスワードを初期パスワード「password123」にリセットしますか？\n\nユーザーには初期パスワードでログイン後、パスワード変更するよう伝えてください。`)) {
        return;
      }
      
      try {
        const response = await fetch(`/admin/users/${userId}/reset-password`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          }
        });
        
        const text = await response.text();
        
        if (response.ok) {
          alert(text);
        } else {
          alert('エラー: ' + text);
        }
      } catch (error) {
        console.error(error);
        alert('エラーが発生しました: ' + error.message);
      }
    }
  </script>
</body>
</html>
